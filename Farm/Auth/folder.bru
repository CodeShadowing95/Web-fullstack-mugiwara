meta {
  name: Auth
}

item {
  name: Register
  request {
    url: "{{baseUrl}}/api/register"
    method: "POST"
    headers: {
      "Content-Type": "application/json"
    }
    body: '{
      "email": "test2@test.com",
      "password": "password",
      "firstName": "John",
      "lastName": "Doe"
    }'
    bodyType: json
  }
}
